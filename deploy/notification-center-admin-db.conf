[Service]
# The protected admin console initializes the shared Notify SQLite schema on
# its first dashboard read, so ProtectSystem must permit this database path.
ReadWritePaths=/var/lib/notification-center
